<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CarCat.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Home">

    <Grid
        Padding="16"
        RowDefinitions="Auto,*"
        RowSpacing="16"
        VerticalOptions="Center">
        <VerticalStackLayout Spacing="8">
            <Image Source="splash.svg" />
            <Label Style="{StaticResource TitleText}" Text="Car Category" />
            <Label Style="{StaticResource BodyText}" Text="Marka, model, yol, motor ve sanziman bilgilerine gore araclari incele." />
        </VerticalStackLayout>

        <VerticalStackLayout Grid.Row="1" Spacing="12">
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
            <Button
                Clicked="OnCarsClicked"
                Style="{StaticResource PrimaryButton}"
                Text="{Binding CarsCountText}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>